def main(df, dataset_results, config):
    """
    主函數，運行T2GFormer模型並返回結果
    Args:
        df: 資料集DataFrame
        dataset_results: 資料集結果
        config: 實驗配置
    Returns:
        dict: 實驗結果
    """
    # 這裡可以添加T2GFormer模型的運行邏輯
    # 例如，使用T2GFormer進行訓練和預測
    print("Running T2GFormer model...")
    print(f"df: {df.shape}")
    # print(f"dataset_results: {dataset_results}")
    print(f"config: {config}")